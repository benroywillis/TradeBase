# Contains path to TradeBase shared object and include directories
get_filename_component(TRADEBASE_CMAKE_DIR "${CMAKE_CURRENT_LIST_FILE}" PATH)
set(TRADEBASE_INCLUDE_DIRS "@CONF_INCLUDE_DIRS@")

#if(NOT TARGET foo AND NOT FooBar_BINARY_DIR)
#    include("${TRADEBASE_CMAKE_DIR}/lib/TradeBase.cmake")
#endif()

# set the install path if it is not default
set(TRADEBASE_LIBRARIES_PATH ${TRADEBASE_CMAKE_DIR}/lib)

set(TRADEBASE_LIBRARIES TradeBase)
